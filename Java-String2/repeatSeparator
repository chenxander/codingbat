public String repeatSeparator(String word, String sep, int count) {
  count--;
  String output = "";
  if (count < 0) return "";
  for (int i = 0; i < count; i++) {
    output = output + word + sep;
  }
  output = output + word;
  return output;
}
